<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Uno Moss</title>

    <link rel="stylesheet" href="/css/form.css">
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/post.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
</head>

<body>
    {{!-- nav bar is same across all pages --}}
    <nav class="navbar">
        <div class="container-fluid">
            <a class="navbar-brand" href="/home">
                <img src="/img/Logo.jpg" alt="Logo" width="50" height="44"
                    class="d-inline-block align-text-middle">
                <h1 class="d-inline-block align-text-top">Uno Moss</h1>
            </a>
        </div>
        {{!-- if logged in, a 'Profile' btn will appear to route the user back to their homepage as needed --}}
        {{!-- if loggged in, a 'Logout' btn will appear --}}
        {{#if logged_in}}
        <a class="d-inline-block align-text-top" href="/home">Profile</a> |
        <a class="d-inline-block align-text-top" id="logout">Logout</a>
        {{/if}}
    </nav>

    {{!-- render sub layouts --}}
    <main class="container container-fluid mt-5">
        {{{ body }}}
    </main>

    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}



</body>

</html>